'use strict';

/**
 * invite-team-member service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::invite-team-member.invite-team-member');
